<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- This is for making the status bar translucent for when the nav drawer opens above it.
            Works only on API 19 and onwards -->
    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowTranslucentStatus">true</item>
        <!-- Colors need to be specified again even though the other resource file already had them defined ??? -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
    </style>

</resources>
<?xml version="1.0" encoding="utf-8"?>
<transitionSet xmlns:android="http://schemas.android.com/apk/res/android"
    android:duration="300"
    android:interpolator="@android:interpolator/fast_out_linear_in"
    android:transitionOrdering="together">
    <targets>
        <target android:excludeId="@id/detail_temp_container"/>
    </targets>

    <!-- TODO: For some reason... parents work too? -->
    <slide android:slideEdge="bottom">
        <targets>
            <target android:targetId="@id/scrollview_details"/>
        </targets>
    </slide>

    <fade>
        <targets>
            <target android:excludeId="@id/scrollview_details"/>
            <target android:excludeId="@id/detail_photo"/>
            <target android:excludeId="@id/detail_temp_photo"/>
        </targets>
    </fade>
</transitionSet>